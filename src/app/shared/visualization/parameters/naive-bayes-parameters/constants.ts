export const GRID_VIEW_THRESHOLD = 12; // Threshold for switching between grid and heatmap views

export const GRID_TOOLTIP_DELAY_DURATION = 300; // Delay duration for tooltips in matrix grid view

export const DEFAULT_PRECISION = 6; // Default precision for displaying numerical values
