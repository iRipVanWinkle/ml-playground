export const EPSILON = 1e-7; // Small value to avoid log(0)

export const DEFAULT_TOLERANCE = 1e-6; // Default tolerance for optimizers

// Constants for controlling the training loop
export const PAUSE_CHECK_INTERVAL_MS = 50;
export const EVENT_LOOP_YIELD_MS = 0;
